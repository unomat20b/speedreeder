name: speedreeder
description: "RSVP speed reader (IntellectShop)"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.3
  file_picker: ^8.1.6
  uuid: ^4.5.1
  path: ^1.9.1
  flutter_web_plugins:
    sdk: flutter
  epubx: ^4.0.0
  html: ^0.15.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
